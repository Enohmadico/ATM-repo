name: ATM CI

on:
  push:
    branches: [ dev ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Display greeting
        run: echo "How are you boy, how is school?"

      - name: Print contents of book.txt
        run: Get-Content ".github/workflows/book.txt"




      